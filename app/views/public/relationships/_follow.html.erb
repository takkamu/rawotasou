<% if current_customer.following?(customer) %>
  <%= link_to "フォロー解除", customer_relationships_path(customer.id), class: "btn btn-light rounded-pill ml-2", method: :delete, remote: true %>
<% else %>
  <%= link_to "フォローする", customer_relationships_path(customer.id), class: "btn btn-light rounded-pill ml-2", method: :post, remote: true %>
<% end %>